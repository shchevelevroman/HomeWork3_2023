package transport;

public class Bus extends Transport {

    public Bus(String brand, String model, int yearStart, String country, String color, int maxSpeed){
        super(brand, model, yearStart, country, color, maxSpeed);
    }
}
